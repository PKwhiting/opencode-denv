{
  "name": "opencode-denv",
  "version": "0.1.0",
  "private": true,
  "description": "opencode plugin and helpers for pinning chat sessions to remote development environments.",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:shell": "bash tests/denv-terminal-shell.test.sh",
    "typecheck": "tsc --noEmit",
    "shellcheck": "shellcheck denv denv-run denv-shell denv-terminal-shell bin/remote-shell tests/denv-terminal-shell.test.sh"
  },
  "devDependencies": {
    "@opencode-ai/plugin": "1.14.20",
    "@types/node": "^22.20.0",
    "typescript": "^5.9.3",
    "vitest": "^4.0.0"
  }
}
